This directory will be periodically used to store recent releases.

If you want to ensure you have the latest version of the code, build it from source.

We hope to get this software onto a proper release repository such as maven central at some point, but we need to do a bit of QA and build-process improvement before we can do that, hence this temporary measure